# Configuration file for loglyze

# Logging settings
VERBOSE=false

# Default entries per page in interactive mode
ENTRIES_PER_PAGE=20

# Default severity levels to detect
# Override or extend this list in your custom configuration
SEVERITY_LEVELS=("ERROR" "WARN" "WARNING" "INFO" "DEBUG" "TRACE" "FATAL" "CRITICAL" "NOTICE" "EMERGENCY" "ALERT")

# Default error threshold for anomaly detection
ERROR_THRESHOLD=100

# Default interval for time-based metrics
DEFAULT_TIME_INTERVAL="hour"

# Default output format
DEFAULT_OUTPUT_FORMAT="text" 